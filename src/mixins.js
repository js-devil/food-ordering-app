export const add = (a, b) => {
  return a + b;
};

export const minus = (a, b) => {
  return a - b;
};

// export default {
//   add(a, b) {
//     return a + b;
//   },
//   minus(a, b) {
//     return a - b;
//   }
// };
